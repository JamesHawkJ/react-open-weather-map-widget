export interface IErrorProps {
    error: string;
};

export interface ITootltipProps {
    showTooltip: boolean;
    setShowTooltip: (value: boolean) => void;
};

export interface IContainerProps {
    showTooltip: boolean;
};
